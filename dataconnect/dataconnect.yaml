specVersion: "v1"
serviceId: "runmateapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "runmateapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
